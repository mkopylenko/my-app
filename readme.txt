Starting the client:
1. Clone the repository:  git clone https://github.com/mkopylenko/my-app.git
2. cd to project's folder
3. Run "npm install"
5. Start the client: npm start (it will be running on localhost:3000)
6. Repos will be listed on open, clicking on repos will open their data
